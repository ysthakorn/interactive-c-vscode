type RCX
# THIS FILE IS AUTOMATICALLY GENERATED
keyword #define

keyword #else

keyword #endif

keyword #error

keyword #if

keyword #if

keyword #ifdef

keyword break

keyword char

keyword continue

keyword else

keyword float

keyword for
doc for for (initial; condition; aftereach) { body; }       For loop.

keyword if
doc if if (condition) { body1; } else { body2; }       If condition is true (non-zero), execute body1. Otherwise execute body2 (optional)

keyword int

keyword long

keyword persistent

keyword return

keyword struct

keyword typedef

keyword union

keyword void

keyword while
doc while while (condition) { body; }       Loop while condition is true (non-zero)

lib A
doc A A       Motor A (1)

lib B
doc B B       Motor B (2)

lib C
doc C C       Motor C (3)

lib allbrake
doc allbrake allbrake()       Puts all motors in brake mode

lib alloff
doc alloff alloff()       Turns all motors off

lib ao
doc ao ao()       Turns all motors off

lib battery_volts
doc battery_volts float battery_volts()       Returns battery voltage

lib beep
doc beep beep()       Makes a quick beep

lib beeper_off
doc beeper_off beeper_off()       Turns the beeper off (use with beeper_on and set_beeper_pitch)

lib beeper_on
doc beeper_on beeper_on()       Turns the beeper on (call set_beeper_pitch first)

lib bk
doc bk bk(int: motor A-C)       Turns motor on in "backwards" direction

lib brake
doc brake brake(int: motor A-C)       Puts motor in brake mode

lib defer
doc defer defer()       Switches to another process now

lib digital
doc digital int digital(int: sensor 1-3)       Returns 1 for touch sensor pressed, 0 for not pressed

lib enable_encoder
doc enable_encoder int enable_encoder(int: sensor 1-3)       Increments that encode for each transition between high & low

lib disable_encoder
doc disable_encoder int disable_encoder(int: sensor 1-3)       Turns off that encoder

lib read_encoder
doc read_encoder int read_encoder(int: sensor 1-3)       Returns the current value of that encoder

lib reset_encoder
doc reset_encoder int reset_encoder(int: sensor 1-3)       Sets the value of that encoder to 0

lib half_enable_encoder
doc half_enable_encoder int half_enable_encoder(int: sensor 1-3)       Encoder will increment after full up down cycle

lib enable_bidir_encoder
doc enable_bidir_encoder int enable_bidir_encoder(int: sensor 1-3)       Turns on quadrature encoder (lego rotation sensor only)

lib encoderwhite
doc encoderwhite int encoderwhite(int: sensor 1-3)       Calibrates bright value for light encoder

lib encoderblack
doc encoderblack int encoderblack(int: sensor 1-3)       Calibrates dark value for light encoder

lib fd
doc fd void fd(int: motor A-C)       Turns motor on in "forwards" direction

lib hog_processor
doc hog_processor hog_processor()       Keeps from switching to another process

lib kill_process
doc kill_process kill_process(int: pid)       Kills process # pid (pid is returned by start_process)

lib light
doc light int light(int: sensor 1-3)       Returns value of light sensor in powered mode (0-1023)

lib light_passive
doc light_passive int light_passive(int: sensor 1-3)       Returns value of light sensor in unpowered mode (0-1023)

lib motor
doc motor motor(int: motor A-C, int: speed -100 to 100)       Turns motor on at given speed

lib mseconds
doc mseconds long mseconds()       Returns the number of milliseconds since the board was reset

lib msleep
doc msleep msleep(long: duration in milliseconds)       Waits for given duration

lib off
doc off off(int: motor 0-3)       Turns motor off

lib poweroff
doc poweroff void poweroff()       Turns the RCX off

lib pressure
doc pressure void pressure(int: sensor)       Returns 0 to 1024 depending on pressure on touch sensor

lib prgm_button
doc prgm_button int prgm_button()       Returns 1 if Prgm button pressed, 0 if not

lib printf
doc printf printf(string: "format", args...)       Prints to the LCD.  Examples: printf("Hello"); printf("%d\n", light(1));

lib reset_system_time
doc reset_system_time reset_system_time()       Resets the system time to 0

lib set_beeper_pitch
doc set_beeper_pitch set_beeper_pitch(float: frequency)       Sets the beeper pitch (use with beeper_on and beeper_off)

lib setticks
doc setticks setticks(int: ticks)       Sets the number of millisecond ticks to give the current process (1-255)

lib sleep
doc sleep sleep(float: duration in seconds)       Waits for given duration

lib sqrt
doc sqrt float sqrt(float)       Square root

lib start_button
doc start_button int start_button()       Returns 1 if View button pressed, 0 if not

lib start_process
doc start_process int start_process(function call)       Starts a new process

lib stop_button
doc stop_button int stop_button()       Returns 1 if Prgm button pressed, 0 if not

lib tone
doc tone tone(float: frequency, float: duration in seconds)       Makes a tone

lib touch
doc touch int touch(int: sensor 1-3)       Returns 1 if touch sensor pressed, 0 if not

lib view_button
doc view_button int view_button()       Returns 1 if View button pressed, 0 if not

